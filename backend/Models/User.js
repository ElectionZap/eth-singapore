class User {
    constructor(id, userName, email, wallet, pollIDs) {
        this.id = id;
        this.userName = userName;
        this.email = email;
        this.wallet = wallet;
        this.pollIDs = pollIDs;
    }
}

export default User;
